"""AI-driven active vulnerability verification layer.

Plans and executes per-CVE verification tests (not version matches) and produces
probe-confirmed Signal objects the fusion gate can pin as ground truth.
"""
